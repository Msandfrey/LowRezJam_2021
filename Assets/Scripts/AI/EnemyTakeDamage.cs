using System.Collections;
using UnityEngine;

namespace IndieWizards.AI
{
    public class EnemyTakeDamage : MonoBehaviour
    {
        enum DamageType { Melee, Poison, Acid };
        void ApplyDamage(float damage, DamageType damageType)
        {

        }
    }
}